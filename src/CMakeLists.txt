FILE(GLOB BEAST_TEST_SRC "*.cpp")
FILE(GLOB BEAST_TEST_HDR "*.hpp")

ADD_EXECUTABLE(beast_test ${BEAST_TEST_SRC} ${BEAST_TEST_HDR} ${COMMON_INCLUDES})
TARGET_LINK_LIBRARIES(beast_test Beast ${Boost_LIBRARIES})

INCLUDE_DIRECTORIES(../beast/include)
INCLUDE_DIRECTORIES(../beast/extras)

